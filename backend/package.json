{
  "name": "github-dashboard-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npm run migrate && node src/server.js",
    "dev": "nodemon --watch src --exec node src/server.js",
    "migrate": "node scripts/runMigrations.js",
    "verify": "node scripts/verify_backend.js",
    "test": "node --test"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
